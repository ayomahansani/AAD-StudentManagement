CREATE DATABASE aad67;

USE aad67;

CREATE TABLE Student (
                         id VARCHAR(255) NOT NULL PRIMARY KEY,
                         name VARCHAR(255),
                         city VARCHAR(255),
                         email VARCHAR(255),
                         level VARCHAR(255)
);
